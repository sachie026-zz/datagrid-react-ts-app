export interface Customer {
  id: string;
  name: string;
  totalProducts: number;
  dueAmount: number;
  title: string;
}

export const substringCheck = (string1: string, string2: string) => {
  return false;
};
